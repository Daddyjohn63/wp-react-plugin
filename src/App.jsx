import React from 'react';

const App = ({ text }) => {
  return (
    <div className="react-demo-app">
      <h1>{text}</h1>
      {/* Add more components or logic here */}
    </div>
  );
};

export default App;
